Recent tweets in <%= @user_name %>'s (<%= @twitter_username %>) timeline:
<br><br>
<img src="<%= @twitter_avatar_url %>" width="50">
<br><br>

<ol>
<% @struct_array.each do |url_struct| %>
  <li>
    <a href="<%= url_struct.address %>"><%= url_struct.address %></a> (<%= url_struct.appearances %> times) 
    Tweets:
    <% url_struct.tweet_objs.each do |tweet_obj| %>
      <%= tweet_obj.text %>,
    <% end %>
  </li>
<% end %>


</ol>

<!-- <br>
======================================
<br>
<ol>  
  <%# @url_array.each do |tweet_urls| %>
    <li><%#= tweet_urls %></li>
  <%# end %>
</ol> -->

<%= form_tag("/", method: "get") do %>
  <%= label_tag(:twitter_username, "Search for:") %>
  <%= text_field_tag(:twitter_username) %>
  <%= submit_tag("Search") %>
<% end %>
